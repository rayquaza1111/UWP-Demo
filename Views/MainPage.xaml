<Page
    x:Class="UWPDemo.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPDemo.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" VerticalScrollMode="Auto">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,32">
                <TextBlock Text="UWP Demo Application"
                          Style="{StaticResource PageTitleTextBlockStyle}"/>
                <TextBlock Text="Explore various UWP features and controls through interactive demonstrations"
                          Style="{StaticResource BodyTextBlockStyle}"
                          Opacity="0.8"/>
            </StackPanel>

            <!-- Navigation Cards -->
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Controls Demo Card -->
                <Border Grid.Row="0" Grid.Column="0"
                       Style="{StaticResource NavigationCardStyle}">
                    <Button Background="Transparent" 
                           BorderThickness="0"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           Click="NavigateToControlsDemo">
                        <StackPanel>
                            <SymbolIcon Symbol="Library" FontSize="48" Margin="0,0,0,16"/>
                            <TextBlock Text="Controls Demo" 
                                     FontSize="18" 
                                     FontWeight="SemiBold"
                                     HorizontalAlignment="Center"
                                     Margin="0,0,0,8"/>
                            <TextBlock Text="Explore various UWP controls and their capabilities"
                                     TextWrapping="Wrap"
                                     HorizontalAlignment="Center"
                                     Opacity="0.8"/>
                        </StackPanel>
                    </Button>
                </Border>

                <!-- Data Binding Demo Card -->
                <Border Grid.Row="0" Grid.Column="1"
                       Style="{StaticResource NavigationCardStyle}">
                    <Button Background="Transparent" 
                           BorderThickness="0"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           Click="NavigateToDataBindingDemo">
                        <StackPanel>
                            <SymbolIcon Symbol="Link" FontSize="48" Margin="0,0,0,16"/>
                            <TextBlock Text="Data Binding" 
                                     FontSize="18" 
                                     FontWeight="SemiBold"
                                     HorizontalAlignment="Center"
                                     Margin="0,0,0,8"/>
                            <TextBlock Text="Demonstrate MVVM patterns and data binding techniques"
                                     TextWrapping="Wrap"
                                     HorizontalAlignment="Center"
                                     Opacity="0.8"/>
                        </StackPanel>
                    </Button>
                </Border>

                <!-- Settings Card -->
                <Border Grid.Row="1" Grid.Column="0"
                       Style="{StaticResource NavigationCardStyle}">
                    <Button Background="Transparent" 
                           BorderThickness="0"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           Click="NavigateToSettings">
                        <StackPanel>
                            <SymbolIcon Symbol="Setting" FontSize="48" Margin="0,0,0,16"/>
                            <TextBlock Text="Settings" 
                                     FontSize="18" 
                                     FontWeight="SemiBold"
                                     HorizontalAlignment="Center"
                                     Margin="0,0,0,8"/>
                            <TextBlock Text="Application settings and information"
                                     TextWrapping="Wrap"
                                     HorizontalAlignment="Center"
                                     Opacity="0.8"/>
                        </StackPanel>
                    </Button>
                </Border>

                <!-- About Card -->
                <Border Grid.Row="1" Grid.Column="1"
                       Style="{StaticResource NavigationCardStyle}">
                    <Button Background="Transparent" 
                           BorderThickness="0"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           Click="ShowAboutDialog">
                        <StackPanel>
                            <SymbolIcon Symbol="Help" FontSize="48" Margin="0,0,0,16"/>
                            <TextBlock Text="About" 
                                     FontSize="18" 
                                     FontWeight="SemiBold"
                                     HorizontalAlignment="Center"
                                     Margin="0,0,0,8"/>
                            <TextBlock Text="Learn about this demo application"
                                     TextWrapping="Wrap"
                                     HorizontalAlignment="Center"
                                     Opacity="0.8"/>
                        </StackPanel>
                    </Button>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>