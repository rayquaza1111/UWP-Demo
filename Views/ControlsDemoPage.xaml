<Page
    x:Class="UWPDemo.Views.ControlsDemoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPDemo.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Navigation Header -->
        <Grid Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0" Click="NavigateBack" VerticalAlignment="Center" Margin="0,0,16,0">
                <SymbolIcon Symbol="Back"/>
            </Button>
            <TextBlock Grid.Column="1" Text="Controls Demo" 
                      Style="{StaticResource PageTitleTextBlockStyle}" 
                      VerticalAlignment="Center" Margin="0"/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" VerticalScrollMode="Auto">
            <StackPanel Margin="24" Spacing="32">

                <!-- Input Controls Section -->
                <StackPanel Spacing="16">
                    <TextBlock Text="Input Controls" Style="{StaticResource SectionHeaderTextBlockStyle}"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,16,16">
                            <TextBlock Text="TextBox:" Margin="0,0,0,4"/>
                            <TextBox PlaceholderText="Enter some text..." Text="{x:Bind ViewModel.SampleText, Mode=TwoWay}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Grid.Row="0" Margin="16,0,0,16">
                            <TextBlock Text="PasswordBox:" Margin="0,0,0,4"/>
                            <PasswordBox PlaceholderText="Enter password..."/>
                        </StackPanel>

                        <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,16,16">
                            <TextBlock Text="ComboBox:" Margin="0,0,0,4"/>
                            <ComboBox PlaceholderText="Select an option..." ItemsSource="{x:Bind ViewModel.ComboBoxItems}" SelectedItem="{x:Bind ViewModel.SelectedComboBoxItem, Mode=TwoWay}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Grid.Row="1" Margin="16,0,0,16">
                            <TextBlock Text="Slider:" Margin="0,0,0,4"/>
                            <Slider Minimum="0" Maximum="100" Value="{x:Bind ViewModel.SliderValue, Mode=TwoWay}" Header="{x:Bind ViewModel.SliderValue, Mode=OneWay}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="0" Grid.Row="2" Margin="0,0,16,0">
                            <TextBlock Text="DatePicker:" Margin="0,0,0,4"/>
                            <DatePicker Date="{x:Bind ViewModel.SelectedDate, Mode=TwoWay}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Grid.Row="2" Margin="16,0,0,0">
                            <TextBlock Text="TimePicker:" Margin="0,0,0,4"/>
                            <TimePicker Time="{x:Bind ViewModel.SelectedTime, Mode=TwoWay}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <!-- Toggle Controls Section -->
                <StackPanel Spacing="16">
                    <TextBlock Text="Toggle Controls" Style="{StaticResource SectionHeaderTextBlockStyle}"/>
                    
                    <StackPanel Orientation="Horizontal" Spacing="32">
                        <CheckBox Content="Enable feature" IsChecked="{x:Bind ViewModel.IsFeatureEnabled, Mode=TwoWay}"/>
                        <ToggleSwitch Header="Notifications" OnContent="On" OffContent="Off" IsOn="{x:Bind ViewModel.NotificationsEnabled, Mode=TwoWay}"/>
                        <RadioButton Content="Option A" IsChecked="{x:Bind ViewModel.OptionA, Mode=TwoWay}"/>
                        <RadioButton Content="Option B" IsChecked="{x:Bind ViewModel.OptionB, Mode=TwoWay}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Progress Controls Section -->
                <StackPanel Spacing="16">
                    <TextBlock Text="Progress Controls" Style="{StaticResource SectionHeaderTextBlockStyle}"/>
                    
                    <StackPanel Spacing="16">
                        <StackPanel>
                            <TextBlock Text="ProgressBar (Determinate):" Margin="0,0,0,4"/>
                            <ProgressBar Value="{x:Bind ViewModel.ProgressValue, Mode=OneWay}" Maximum="100"/>
                        </StackPanel>
                        
                        <StackPanel>
                            <TextBlock Text="ProgressRing (Indeterminate):" Margin="0,0,0,4"/>
                            <ProgressRing IsActive="{x:Bind ViewModel.IsProgressActive, Mode=OneWay}" Width="32" Height="32" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <!-- Button Controls Section -->
                <StackPanel Spacing="16">
                    <TextBlock Text="Button Controls" Style="{StaticResource SectionHeaderTextBlockStyle}"/>
                    
                    <StackPanel Orientation="Horizontal" Spacing="16">
                        <Button Content="Standard Button" Click="{x:Bind ViewModel.ShowMessage}"/>
                        <Button Content="Accent Button" Style="{ThemeResource AccentButtonStyle}" Click="{x:Bind ViewModel.IncrementProgress}"/>
                        <ToggleButton Content="Toggle Button" IsChecked="{x:Bind ViewModel.IsToggled, Mode=TwoWay}"/>
                        <AppBarButton Icon="Add" Label="Add Item" Click="{x:Bind ViewModel.AddItem}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Media Controls Section -->
                <StackPanel Spacing="16">
                    <TextBlock Text="Media & Content" Style="{StaticResource SectionHeaderTextBlockStyle}"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,16,0">
                            <TextBlock Text="Image:" Margin="0,0,0,4"/>
                            <Border Background="{ThemeResource SystemControlBackgroundAltHighBrush}" Height="120" Width="120" CornerRadius="8">
                                <FontIcon Glyph="&#xE91B;" FontSize="48" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                            </Border>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="16,0,0,0">
                            <TextBlock Text="RichTextBlock:" Margin="0,0,0,4"/>
                            <RichTextBlock MaxWidth="200" TextWrapping="Wrap">
                                <Paragraph>
                                    <Run Text="This is a "/>
                                    <Run Text="RichTextBlock" FontWeight="Bold" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                                    <Run Text=" with "/>
                                    <Run Text="formatted text" FontStyle="Italic"/>
                                    <Run Text=" support."/>
                                </Paragraph>
                            </RichTextBlock>
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <!-- Status Display -->
                <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" 
                       Padding="16" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Current Status:" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <TextBlock Text="{x:Bind ViewModel.StatusText, Mode=OneWay}" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>