<Page
    x:Class="UWP_Demo.Views.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <StackPanel Margin="24" Spacing="24" HorizontalAlignment="Center" MaxWidth="600">
            
            <!-- About: App branding header -->
            <StackPanel Spacing="16" HorizontalAlignment="Center">
                <!-- App icon placeholder -->
                <Border Background="{ThemeResource AccentFillColorDefaultBrush}"
                        CornerRadius="16"
                        Width="80"
                        Height="80"
                        HorizontalAlignment="Center">
                    <SymbolIcon Symbol="People" 
                                Foreground="White"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                </Border>
                
                <StackPanel HorizontalAlignment="Center" Spacing="4">
                    <TextBlock Text="Customer Management" 
                               FontSize="32" 
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                    <TextBlock Text="Version 1.0.0" 
                               FontSize="16" 
                               HorizontalAlignment="Center"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
            </StackPanel>

            <!-- App description -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="24">
                
                <StackPanel Spacing="16">
                    <TextBlock Text="About This App" 
                               FontSize="20" 
                               FontWeight="SemiBold"
                               Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                    
                    <TextBlock Text="Customer Management is a modern UWP application designed to help you efficiently manage customer relationships. Built with the latest Windows development technologies, it provides a seamless and intuitive experience for organizing customer data."
                               TextWrapping="Wrap"
                               LineHeight="24" />
                </StackPanel>
            </Border>

            <!-- Copyright -->
            <TextBlock Text="Copyright 2024 Customer Management App. Built using UWP and WinUI 2.0" 
                       FontSize="12" 
                       HorizontalAlignment="Center"
                       Opacity="0.6"
                       TextAlignment="Center"
                       Margin="0,24,0,0" />
            
        </StackPanel>
    </ScrollViewer>
</Page>